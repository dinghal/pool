# pool

适用于服务器端的进程池和线程池实现

process pool:
半同步/半异步并发模式的进程池。
为了避免在父、子进程中传递文件描述符，本代码将接受新连接的操作放到子进程中来，显然，这种模式下对于一个客户的连接始终只能在一个进程中进行处理。

thread pool:
基于半同步/半反应堆并发模式实现的线程池。
该线程池的通用性较高：
因为使用一个任务队列完全解除了主线程和工作线程的耦合关系：主线程往任务队列中添加任务，工作线程通过竞争来取得任务并执行。
